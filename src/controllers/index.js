module.exports = {

    

    // const conn = require('../db/db.js');
    // var user = require('../models/usuariosDAO.js');
    // var usuariosDAO = new src.models.usuariosDAO(conn);

    
    // function login(req, res) {
    //     let user = usuariosDAO.get(req, res, function (error, results) {
    //         if(error) res.json(error);
    //         res.redirect('/'); 
    //     });

              
    // }

    // function register(req, res){

    //     usuariosDAO.post(req, res, function (error, results) {
    //         console.log(error);
    //         res.redirect('/');
    //     });
    // }


} 

